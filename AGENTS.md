# 项目维护约定

本项目按所有者要求对齐「码伴 CodeCompanion」的长期维护与 GitHub 发布标准，范围限于 macOS 菜单栏应用。

- 先阅读 docs/maintenance.md 和 docs/versioning.md；修复实际问题，不为凑版本新增功能。
- 不覆盖未提交修改、现有用户配置或有效历史标签。没有产品行为变化的文档、配图、测试、构建检查不独立升级版本，不增加迭代次数。
- 每次同步 GitHub 同步 CHANGELOG.md；相对上一公开版本固定记录「新增内容」「Bug 修复」「改进」「验证与升级说明」，无变化写“无”。Bug 写明触发条件、原表现和修复结果。
- 保守使用三段版本：重大不兼容或核心工作流重大扩展才升主版本；兼容功能与局部界面改进升次版本；只修 Bug 升修订号。不得按提交次数或文件数升版本。
- 发布时同步 VERSION、README、CHANGELOG、docs/downloads.md、固定标签和 Release；项目版本与 App 构建号独立管理。未创建的标签、Release 不得声称已发布。
- 一批产品变化只计一次有效发布。旧版本不重排、不复用、不覆盖；历史未打标签的版本引用固定提交。
- UI 改动在对应日志旁附 1～3 张本项目图片，放 docs/assets/，文件名含版本；注明演示数据／离屏渲染／真实截图。保留生成脚本，不使用真实余额、任务或密钥，不把参考项目图片当成本项目。
- 执行 scripts/check_public.py、scripts/check.sh 与必要构建；编译和离屏渲染不能替代桌面交互验收。发布前比较 origin/main，发布后核对远端内容。
- 不发布凭据、缓存、日志、数据库、个人目录、签名身份或账号记录；保留许可证和来源说明。
- 维护 Codex Pro/Plus 实际窗口、DeepSeek 官方多币种余额、深色菜单一致性和缓存错误提示。额度查询不调用付费生成模型。
